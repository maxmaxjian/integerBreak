#include <iostream>
#include <vector>

using std::vector;

class solution {
public:
  int integerBreak(int n) {
    
  }

private:
  vector<int> maxProd(int n, int numOfInts) {
    vector<int> result;
    if (numOfInts == 1) {
      result.push_back(n);
    }
    else {
      vector<vector<int>> cands;
       
    }
  }
};
